# workflows/ — legacy, scheduled for deletion

These are ComfyUI workflow graphs from the retired local backend. ComfyUI is gone from the
machine and nothing runs them. They remain only because the current unit suite uses them as
fixtures; roadmap milestone M1 deletes this folder along with `src/comfy.ts`,
`src/workflows.ts`, and `src/tools/upscale.ts`.

Do not extend, edit, or document them. See [../ROADMAP.md](../ROADMAP.md).
